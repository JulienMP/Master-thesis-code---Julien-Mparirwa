# Project paths
PROJECT_ROOT="/path/to/your/project"
VENV_PATH="/path/to/your/virtual/environment"
VISUAL_VENV_PATH="/path/to/your/visual/environment"  # Different env for SlowFast/PyTorchVideo
BYTETRACK_HOME="/path/to/your/project/src/tracking"

# Data directories
DATA_DIR="/path/to/data/storage"
LOG_DIR="/path/to/log/files"
CLIPS_OUTPUT_DIR="/path/to/clips/output"
ORGANIZED_CLIPS_DIR="/path/to/organized/clips"
SUBSET_OUTPUT_DIR="/path/to/subset/output"
TRACKING_INPUT_DIR="/path/to/videos/for/tracking"
TRACKING_OUTPUT_DIR="/path/to/tracking/results"
TRACKING_FEATURES_OUTPUT_DIR="/path/to/tracking/features"
CROWD_BEHAVIOR_OUTPUT_DIR="/path/to/crowd/behavior/features"
CROWD_STATISTICAL_OUTPUT_DIR="/path/to/crowd/statistical/features"
VISUAL_INPUT_DIR="/path/to/video/clips"
VISUAL_FEATURES_OUTPUT_DIR="/path/to/visual/features"
TRAINING_OUTPUT_DIR="/path/to/training/results"
TESTING_OUTPUT_DIR="/path/to/testing/results"

# SoccerNet download settings
SOCCERNET_PASSWORD="s0cc3rn3t"

# Extraction settings
EXTRACTION_TYPE="goals"  # goals, background, freekicks, penalties, shots
CLIPS_PER_GAME=3         # Number of background clips per game
FREEKICK_WINDOW=10       # Seconds before goal to look for free kick
PENALTY_WINDOW=120       # Seconds before penalty to look for trigger event

# Subset creation settings
SUBSET_TOTAL_VIDEOS=4000      # Total number of videos in subset
SUBSET_BACKGROUND_PROP=0.25   # 25% background clips
SUBSET_BEFORE_GOAL_PROP=0.28  # 28% before goal clips
SUBSET_FREE_KICKS_PROP=0.025  # 2.5% free kick clips
SUBSET_PENALTIES_PROP=0.028   # 2.8% penalty clips
SUBSET_SHOTS_NO_GOALS_PROP=0.417  # 41.7% shots without goals clips

# Tracking settings
TRACKING_EXP_FILE="/path/to/yolox_x_soccernet.py"
TRACKING_CHECKPOINT="/path/to/bytetrack_x_mot20.tar"
TRACKING_TEST_SIZE=1088
TRACKING_DEVICE="cuda"  # cuda or cpu
TRACKING_ENV_NAME="envsoccer"  # Name of conda environment for tracking

# Visual processing settings
VISUAL_DEVICE="cuda"  # cuda or cpu
VISUAL_ENV_NAME="envslowfast"  # Name of conda environment for visual features

# Training settings
TRAINING_FUSION_DIM=256      # Fusion layer dimension
TRAINING_NUM_CLUSTERS=5      # Number of clusters for multi-task learning
TRAINING_EPOCHS=50           # Number of training epochs
TRAINING_BATCH_SIZE=32       # Training batch size
TRAINING_DEVICE="cuda"       # cuda or cpu

# Testing settings
TESTING_DEVICE="cuda"        # cuda or cpu

# Embeddings preprocessing settings
EMBEDDINGS_OUTPUT_DIR="/path/to/embeddings/output"
EMBEDDINGS_DEVICE="cuda"     # cuda or cpu

# Runtime prediction settings
PREDICTION_OUTPUT_DIR="/path/to/prediction/results"